import pytest

pytest.skip("Lesson 04 Part 4 — not implemented yet", allow_module_level=True)


def test_qp_matches_dls_when_unconstrained():
    """QPDiffIK.step output matches the DLS feedback controller's qdot to ~1e-6 when no constraint is active — it's the same optimization. Verified by: Part 4 checkpoint (baseline comparison)."""
    raise NotImplementedError
