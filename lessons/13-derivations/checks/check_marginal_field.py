import pytest

pytest.skip("Lesson 13 Part 2 — not implemented yet", allow_module_level=True)


def test_trained_field_matches_analytic_marginal_field():
    """A tiny MLP trained on the CFM objective over the 1-D Gaussian-mixture target
    matches the analytic marginal field u_t(x) on a (t, x) grid; max grid error < 0.05
    where p_t(x) > 1e-3 (Part 2 checkpoint)."""
    raise NotImplementedError
