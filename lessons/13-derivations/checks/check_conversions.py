import pytest

pytest.skip("Lesson 13 Part 3 — not implemented yet", allow_module_level=True)


def test_converted_models_agree_with_directly_trained():
    """An epsilon-model trained on the Part 2 mixture, converted to a score model and a
    velocity model via the Part 3 conversion table, agrees with directly-trained
    score/velocity counterparts within training noise on the grid (Part 3 checkpoint)."""
    raise NotImplementedError
