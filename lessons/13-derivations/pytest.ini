[pytest]
; README "Done when" runs `pytest checks/`; the numerical-check files are named
; check_*.py (per Parts 1-3), not test_*.py, so widen pytest's default collection
; pattern to pick them up.
python_files = check_*.py test_*.py
