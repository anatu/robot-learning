# Lesson 11 Part 2/3 stub — spec in README.md "Part 2 — The sweep and the heatmap" and
# "Part 3 — AutoDR-lite". Implement yourself. Claude scaffolds, reviews, and verifies only —
# see TEMPLATE.md execution contract.

env:
  id: Pusher-v5  # gymnasium[mujoco]

fixed_width_sweep:
  widths: [1.0, 1.5, 2.5, 4.0]  # TODO
  seeds: [0, 1, 2]  # TODO
  total_env_steps: 300000  # TODO

eval_grid:
  mass_multiplier_range: [0.25, 4.0]  # TODO
  friction_multiplier_range: [0.25, 4.0]  # TODO
  points_per_axis: 7  # TODO: 7 log-spaced points per axis -> 49 cells
  episodes_per_cell: 20  # TODO
  committed_before_training: true  # TODO: this file's grid must predate training runs in git history

autodr_lite:
  initial_width: 1.1  # TODO
  eval_every_steps: 10000  # TODO
  boundary_episodes: 20  # TODO (Pitfalls: drop to 10 if wall-clock dominated)
  expand_threshold: 0.7  # TODO: boundary success >= this -> push that bound outward 10%
  shrink_threshold: 0.3  # TODO: boundary success < this -> pull that bound inward 10%
