#!/usr/bin/env bash
# Lesson 11 Part 2 stub — spec in README.md "Part 2 — The sweep and the heatmap".
# Implement yourself. Claude scaffolds, reviews, and verifies only — see TEMPLATE.md execution
# contract.
#
# TODO: train all fixed-width arms (configs/sweep_config.yaml's `fixed_width_sweep`), train the
# AutoDR-lite arm (`autodr_lite`), then evaluate every trained policy on the committed 49-cell
# eval grid and render the heatmaps.

set -euo pipefail

echo "TODO: not implemented — see README.md Part 2" >&2
exit 1
